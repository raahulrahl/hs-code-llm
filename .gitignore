# Python
__pycache__/
*.py[cod]
*.so
.venv/
.uv/
*.egg-info/

# Training outputs
checkpoints/
runs/
wandb/
logs/
outputs/
*.pt
*.bin
*.safetensors

# Loose dataset files at data/ root are not tracked — snapshots live
# under data/<YYYY-MM-DD>/ and the data/current symlink points at one.
data/*.jsonl
data/*.json

# Notebook outputs (commit clean notebooks only)
.ipynb_checkpoints/

# Secrets — see .env.example for the shape, NEVER commit .env
.env
.env.local

# OS
.DS_Store
